class Solution(object):
    def longestCommonPrefix(self, strs):
        dicto = []
        for i in range(0, len(strs)):
            dicto[i, strs[i]]
        print(dicto)


sol = Solution()
sol.longestCommonPrefix(["flow", "dsads", "rede"])
